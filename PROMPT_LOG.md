# PROMPT_LOG — Лабораторная работа №12

**Студент:** Кипчатов Максим Маратович  
**Группа:** 221331  
**Вариант:** 26 — Сервис аренды квартир

---

## Задание 1 — Веб-приложение

При разработке приложения использовал Claude Code как вспомогательный инструмент для генерации шаблонного кода. Основную архитектуру (FastAPI + SQLAlchemy + Jinja2) выстраивал сам, ИИ помогал с рутинными частями.

### Промпты

**Промпт 1:**
```
Напиши SQLAlchemy-модели для сервиса аренды квартир. 
Нужны модели: User (роли tenant/landlord/admin), Apartment, Booking (статусы pending/confirmed/cancelled/completed), Payment (эскроу: held/released/refunded), Review (рейтинг 1-5).
```

**Ответ ИИ:** Сгенерировал модели с нужными полями и enum-статусами. Добавил relationship между таблицами.

**Что исправил:** Добавил `is_active` в модель `User` вручную — ИИ его пропустил. Также добавил CHECK constraint на рейтинг в модели `Review`.

---

**Промпт 2:**
```
Напиши сервисный слой для работы с квартирами: create_apartment, get_apartment, get_apartments с фильтрами по городу и цене, update_apartment, delete_apartment (мягкое удаление через is_active).
```

**Ответ ИИ:** Сгенерировал функции с SQLAlchemy-запросами. Фильтрация по городу через `ilike`, мягкое удаление через флаг `is_active`.

**Что исправил:** Добавил фильтр `Apartment.is_active == True` в `get_apartments` — ИИ его забыл, неактивные квартиры показывались в листинге.

---

**Промпт 3:**
```
Напиши FastAPI-роутер для квартир с эндпоинтами: GET /apartments/, GET /apartments/{id}, POST /apartments/ (только landlord), PUT /apartments/{id} (владелец или admin), DELETE /apartments/{id}.
```

**Ответ ИИ:** Сгенерировал роутер с зависимостями `get_current_user` и `require_landlord`.

**Что доработал:** Дописал проверку прав на редактирование — ИИ не учёл, что admin тоже должен иметь доступ к чужим объявлениям.

---

**Промпт 4:**
```
Напиши Jinja2-шаблоны для главной страницы, страницы квартиры, профиля пользователя и панели администратора. Bootstrap 5, без внешних CDN.
```

**Ответ ИИ:** Сгенерировал базовый шаблон `base.html` с навигацией и дочерние шаблоны.

**Что исправил:** Поправил навигацию — ссылки на создание объявления показывались всем пользователям, хотя должны только landlord и admin. Доработал шаблон профиля — добавил секцию для арендодателя с входящими бронями.

---

**Промпт 5:**
```
Реализуй JWT-аутентификацию: регистрация, вход, защищённые эндпоинты через Depends. Пароли через bcrypt.
```

**Ответ ИИ:** Использовал `passlib` для хэширования, `python-jose` для JWT.

**Что исправил:** `passlib` оказался несовместим с `bcrypt 5.x` на Python 3.14 — при регистрации выбрасывало ошибку "password cannot be longer than 72 bytes". Переписал `security.py` вручную: убрал `passlib`, добавил SHA-256 предхэш (`hashlib.sha256`) перед передачей в `bcrypt`. Также исправил JWT: `sub` должен быть строкой (`str(user.id)`), а при декодировании конвертировать обратно в `int`.

---

## Задание 2 — Code Review

Провёл ревью кода, сгенерированного ИИ в задании 1. Смотрел на безопасность, логику и качество кода. Нашёл 5 проблемных мест.

### Промпты

**Промпт 1:**
```
Выступи в роли senior-разработчика. Проведи code review следующего кода сервиса аутентификации. Найди баги, проблемы безопасности и недочёты:
[вставил код authenticate_user и security.py]
```

**Ответ ИИ:** Указал на то, что функция `authenticate_user` не проверяет флаг `is_active` — заблокированный пользователь мог войти в систему.

**Что сделал:** Добавил проверку `not user.is_active` в условие отказа в аутентификации.

---

**Промпт 2:**
```
Проверь функцию create_review. Есть ли проблемы с бизнес-логикой?
[вставил код review_service.py]
```

**Ответ ИИ:** Обнаружил, что нет проверки на дублирующийся отзыв — один пользователь мог оставить несколько отзывов на одну квартиру через прямые запросы к API.

**Что сделал:** Добавил функцию `has_existing_review()` и проверку перед созданием отзыва.

---

**Промпт 3:**
```
Проверь логику проверки доступности квартиры check_availability. Корректно ли определяется пересечение дат бронирования?
```

**Ответ ИИ:** Первоначальная реализация использовала три условия OR для определения пересечения, что избыточно. Предложил упрощённую формулу: `check_in < checkout AND check_out > checkin`.

**Что сделал:** Упростил логику, убедился в корректности на граничных случаях (заезд в день выезда другого — не конфликт).

---

**Промпт 4:**
```
Посмотри на эндпоинт GET /bookings/{booking_id}. Правильно ли настроены права доступа?
```

**Ответ ИИ:** Указал, что доступ к чужому бронированию был открыт любому авторизованному пользователю — не было проверки, является ли пользователь арендатором, арендодателем квартиры или администратором.

**Что сделал:** Добавил проверку `is_tenant`, `is_landlord` и `current_user.role == admin`.

---

**Промпт 5:**
```
Проверь работу со статическими файлами и Starlette 1.0. Есть ли проблемы совместимости?
```

**Ответ ИИ:** Обнаружил устаревший синтаксис `TemplateResponse` — в Starlette 1.0 изменился порядок аргументов: `request` теперь первый аргумент, а не часть контекста.

**Что сделал:** Обновил все вызовы `TemplateResponse` в `web/router.py` под новый API.

---

## Задание 3 — Настройка локальной LLM

Настроил локальную языковую модель и сравнил её с облачным решением на реальных задачах из проекта.

### Установка и настройка

Ollama у меня уже был установлен. Скачал модель:

```bash
ollama pull qwen3.5:27b
```

Установил расширение Continue.dev для VS Code:

```bash
code --install-extension Continue.continue
```

Настроил конфиг `~/.continue/config.json`:

```json
{
  "models": [
    {
      "title": "Qwen 3.5 27B (Local)",
      "provider": "ollama",
      "model": "qwen3.5:27b",
      "apiBase": "http://localhost:11434"
    }
  ],
  "tabAutocompleteModel": {
    "title": "Qwen 3.5 27B Autocomplete",
    "provider": "ollama",
    "model": "qwen3.5:27b",
    "apiBase": "http://localhost:11434"
  }
}
```

Столкнулся с проблемой: модель по умолчанию включает режим "размышления" (`<think>`), который съедает весь лимит токенов и не выдаёт ответа. Решение — передавать параметр `"think": false` в запросах к Ollama API.

### Промпты и результаты

**Промпт 1 (CRUD):**
```
Напиши функцию get_available_apartments(db, city, check_in, check_out) на SQLAlchemy, которая возвращает квартиры без пересекающихся бронирований на указанные даты.
```

| | Qwen3.5:27b (локальная) | Claude Sonnet (облачная) |
|---|---|---|
| Время ответа | 98.4 сек | ~3 сек |
| Результат | Рабочий код, `.exists()` | Type hints, `ilike`, enum, `is_active` |

---

**Промпт 2 (SQL):**
```
Напиши SQL-запрос: топ-5 квартир по количеству завершённых бронирований за последние 30 дней.
```

| | Qwen3.5:27b (локальная) | Claude Sonnet (облачная) |
|---|---|---|
| Время ответа | 66.9 сек | ~2 сек |
| Результат | SQLite-синтаксис (`DATE('now', '-30 days')`) | Стандартный SQL `INTERVAL` |

---

**Промпт 3 (Regex):**
```
Напиши функцию validate_phone(phone: str) -> bool для формата +7XXXXXXXXXX.
```

| | Qwen3.5:27b (локальная) | Claude Sonnet (облачная) |
|---|---|---|
| Время ответа | 31.6 сек | ~1 сек |
| Результат | `re.match`, лаконично | Скомпилированный паттерн, docstring |

### Выводы

Локальная модель справилась со всеми задачами корректно, но работает в ~30 раз медленнее. Для интерактивного автодополнения в IDE задержка 30–100 секунд неприемлема. Основное преимущество локальной LLM — конфиденциальность: код не покидает машину.

---

## Задание 4 — CI/CD GitHub Actions

Настроил автоматический анализ Pull Request через Claude API.

### Промпт

```
Напиши GitHub Actions воркфлоу, который при открытии или обновлении PR:
1. Получает diff изменённых файлов (.py, .html, .yml, .json, .md)
2. Отправляет diff в Claude API с просьбой описать изменения на русском
3. Постит ответ как комментарий к PR
```

**Ответ ИИ:** Сгенерировал воркфлоу с шагами: `actions/checkout`, получение diff через `git diff`, запрос к Anthropic API через `curl`, публикация комментария через `actions/github-script`.

**Что доработал:** 
- Ограничил diff первыми 12000 символами, чтобы не превышать лимит токенов
- Добавил фильтрацию только по нужным расширениям файлов
- Добавил fallback-сообщение если API недоступен

Воркфлоу требует секрет `ANTHROPIC_API_KEY` в настройках репозитория (Settings → Secrets → Actions).

---

## Задание 7 — Unit-тесты

Написал тесты для всех модулей приложения с целью достичь покрытия ≥ 90%.

### Промпт 1:
```
Напиши pytest-фикстуры для тестирования FastAPI-приложения с SQLAlchemy. 
Нужна изолированная SQLite in-memory БД, TestClient с подменённой зависимостью get_db, фикстуры пользователей (tenant, landlord, admin), квартиры и бронирования.
```

**Ответ ИИ:** Предложил стандартный подход с переопределением `get_db`. Столкнулся с проблемой: in-memory SQLite при каждом соединении создаёт новую БД — таблицы, созданные в одном соединении, не видны в другом.

**Что исправил:** Добавил `poolclass=StaticPool` из `sqlalchemy.pool` — это заставляет SQLAlchemy переиспользовать одно соединение для всех сессий.

---

**Промпт 2:**
```
Напиши тесты для модуля security.py: хэширование паролей, верификация, JWT.
Включи тест на пароли длиннее 72 байт (bcrypt truncation).
```

**Ответ ИИ:** Сгенерировал тесты. Подсказал протестировать именно граничный случай с длинными паролями — это прямо связано с нашим SHA-256 предхэшем.

---

**Промпт 3:**
```
Напиши API-тесты для эндпоинтов /apartments/. Покрой: список с фильтрами, получение по id, создание (права по ролям), обновление, удаление.
```

**Ответ ИИ:** Сгенерировал тесты. Часть тестов на права доступа пришлось дописать вручную — ИИ не покрыл случай admin может редактировать чужое объявление.

### Итог

```
136 тестов — все прошли
Покрытие: 98%
```
